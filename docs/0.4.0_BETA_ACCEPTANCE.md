# Zotero Pi Assistant 0.4.0-beta Upgrade Acceptance

Date: 2026-08-25

## Installed artifact

- Add-on: `Zotero Pi Assistant 0.4.0-beta`
- Add-on ID: `zotero-agent-bridge@local`
- XPI SHA-512: `8652cf8de62cb80daf07f23103fd7177b5d4010ad525532c53d847c8dd7dde1831e71897858ec005d5188ff07ffdec1c7914f7eef4a334b34da912d74a4905ee`
- Bridge lifecycle protocol: `2`
- Product scope: `zotero-pi-only`
- Distribution: `xpi-bundled`

The previous `0.3.5` XPI was backed up before replacement. Zotero was closed before the profile XPI was copied.

## Upgrade checks

- Bundled `0.4.0-beta` Bridge installed and launched from the managed binary root.
- `install-state.json` records `current_version=0.4.0-beta`, protocol `2`, product scope `zotero-pi-only`, and `protocol_floor=2`.
- Managed configuration migrated from schema 1 to schema 2.
- Bridge home and Pi session directory remained unchanged.
- API token file hash was unchanged across upgrade.
- Pi session index file hash was unchanged immediately after upgrade.
- All three existing Pi JSONL session files remained present with the same total size immediately after upgrade.
- The add-on reported `ready`, `bridge_state=ready`, and owned Bridge lifecycle.
- The add-on startup sequence created the Pi panel controller before logging `addon_started`.

## Real paper/session checks

Using an existing Zotero paper:

- `POST /assistant/session/open` resolved the expected item, PDF attachment, 23 pages, and the existing document ID prefix.
- `GET /assistant/session/history` returned the current session, an archived session, and the previously orphaned session.
- The orphaned session was resumed successfully and exposed its prior messages.
- The original current session was then restored successfully and exposed 24 prior user/assistant messages.
- No session JSONL file was deleted. Resuming history intentionally updated the session index and appended normal Pi session metadata.

## Retirement checks

- A real `POST /items` request returned authenticated HTTP `410 feature_retired`.
- The add-on command queue remained unchanged, confirming no Zotero write side effect.
- The public `updates.json` remains on the already-published `0.3.5` asset. `0.4.0-beta` is an installed release candidate, not a published automatic update.

## Automated evidence

- 126 tests passed.
- Python `compileall` passed.
- `git diff --check` passed.
- The packaged EXE passed authenticated `/health`, protocol-v2 `/lifecycle`, and owner-authenticated shutdown smoke tests.
- XPI resource, manifest, SBOM, hash, machine-path, and retired-module scans passed.

## Deferred removal

Stage-2 physical deletion of the authenticated HTTP 410 shells and CLI compatibility shells is intentionally deferred to `0.4.1-beta`, after the transition release has been published and observed stable.
